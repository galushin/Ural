addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - g++-5.1
            - gcc-5.1

language: cpp
compiler: gcc

script: cd ./ural_test && make -f makefile.gcc release
